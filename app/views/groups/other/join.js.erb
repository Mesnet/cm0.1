$('#other_group<%=@group.id%>').slideUp();
$('#item_list2').append("<%=  j render partial: 'groups/group', locals: {group: @group} %>");
$('#group_iter<%=@group.id%>').hide();
$('#group_iter<%=@group.id%>').slideDown();
// Scroll Bottom
var height = $(list)[0].scrollHeight;
$(list).scrollTop(height);