$('#company_info_format').remove();
$('#company_invitation_format').remove();
$('body').append("<%= j render partial: 'devise/registrations/user_info_registring' %>");
$('#user_info_format').hide();
$('#user_info_format').fadeIn();
window.history.pushState('page2', 'Title', '/user_info_register');